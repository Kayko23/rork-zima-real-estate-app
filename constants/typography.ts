const Type = {
  h1: { fontSize: 30, lineHeight: 36, fontWeight: "800" as const }, // ZIMA
  h2: { fontSize: 22, lineHeight: 28, fontWeight: "800" as const }, // titres sections
  h3: { fontSize: 18, lineHeight: 24, fontWeight: "700" as const }, // cartes (labels)
  body: { fontSize: 16, lineHeight: 22, fontWeight: "600" as const }, // textes/CTA
  small: { fontSize: 13, lineHeight: 18, fontWeight: "600" as const }, // badges/puces
  tiny: { fontSize: 12, lineHeight: 16, fontWeight: "600" as const },
};

export default Type;